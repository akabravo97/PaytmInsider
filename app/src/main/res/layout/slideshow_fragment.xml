<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:attrs="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <pl.pzienowicz.autoscrollviewpager.AutoScrollViewPager
        android:id="@+id/slideShowViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="5dp"
        android:layout_marginRight="5dp"
        android:clipToPadding="false"
        android:elevation="20dp"></pl.pzienowicz.autoscrollviewpager.AutoScrollViewPager>

    <com.rd.PageIndicatorView
        android:id="@+id/pageIndicatorView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:elevation="25dp"
        app:piv_animationType="scale"
        app:piv_dynamicCount="true"
        app:piv_interactiveAnimation="true"
        app:piv_selectedColor="@color/blue_btn_bg_color"
        app:piv_unselectedColor="@color/red_btn_bg_color"
        app:piv_viewPager="@id/slideShowViewPager"
        attrs:piv_padding="12dp"
        attrs:piv_radius="8dp" />

</RelativeLayout>